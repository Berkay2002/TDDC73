---
title: Getting Started
description: Learn how to install and start using Primitive UI in your Flutter project
---

import { Steps, Callout, Tabs } from 'nextra/components'

# Getting Started

Get up and running with Primitive UI in your Flutter project in just a few minutes.

## What is Primitive UI?

Primitive UI is a Flutter GUI library built entirely from scratch using only primitive components:

- **CustomPaint** and **Canvas** for rendering
- **GestureDetector** for user input
- **Custom RenderBox** for layout

No high-level widgets like `Card`, `Column`, `Switch`, or `Stack` are used in the implementation. This makes Primitive UI an excellent educational tool for understanding how Flutter's rendering engine works under the hood.

## Prerequisites

Before you begin, ensure you have:

<Callout type="info">
**Required:**
- Flutter SDK `>=3.9.2`
- Dart SDK (comes with Flutter)
- A code editor (VS Code, Android Studio, or IntelliJ IDEA)
</Callout>

### Verify Flutter Installation

Check your Flutter installation:

```bash
flutter --version
```

You should see output similar to:

```
Flutter 3.9.2 • channel stable
Tools • Dart 3.0.0
```

## Installation

<Steps>

### Add Primitive UI to Your Project

Since Primitive UI is currently a local package, add it to your `pubspec.yaml`:

```yaml filename="pubspec.yaml" {3-4}
dependencies:
  flutter:
    sdk: flutter
  primitive_ui:
    path: ../primitive_ui
```

<Callout type="warning">
**Important:** Adjust the `path` to match your project structure. The path is relative to your project's root directory.
</Callout>

### Install Dependencies

Run the following command in your terminal:

```bash
flutter pub get
```

You should see output like:

```
Running "flutter pub get" in your_project...
Resolving dependencies...
+ primitive_ui 0.0.1 from path ../primitive_ui
Changed 1 dependency!
```

### Import the Library

In your Dart files, import Primitive UI:

```dart filename="lib/main.dart"
import 'package:primitive_ui/primitive_ui.dart';
```

### Verify Installation

Create a simple test to verify everything works:

```dart filename="lib/main.dart"
import 'package:flutter/material.dart';
import 'package:primitive_ui/primitive_ui.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: CustomCard(
            child: Text('Primitive UI Works!'),
          ),
        ),
      ),
    );
  }
}
```

Run your app:

```bash
flutter run
```

You should see a white card with "Primitive UI Works!" in the center.

</Steps>

## First Example

Let's create a simple settings panel using Primitive UI components:

<Tabs items={['Complete Example', 'Step by Step']}>
  <Tabs.Tab>
```dart filename="lib/main.dart"
import 'package:flutter/material.dart';
import 'package:primitive_ui/primitive_ui.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Primitive UI Demo',
      home: SettingsPage(),
    );
  }
}

class SettingsPage extends StatefulWidget {
  @override
  State<SettingsPage> createState() => _SettingsPageState();
}

class _SettingsPageState extends State<SettingsPage> {
  bool _notificationsEnabled = true;
  bool _darkModeEnabled = false;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Settings')),
      body: Padding(
        padding: EdgeInsets.all(16.0),
        child: VStack(
          spacing: 16.0,
          children: [
            CustomCard(
              elevation: 2.0,
              child: Row(
                mainAxisAlignment: MainAxisAlignment.spaceBetween,
                children: [
                  Text('Notifications'),
                  CustomToggleSwitch(
                    value: _notificationsEnabled,
                    onChanged: (value) {
                      setState(() => _notificationsEnabled = value);
                    },
                  ),
                ],
              ),
            ),
            CustomCard(
              elevation: 2.0,
              child: Row(
                mainAxisAlignment: MainAxisAlignment.spaceBetween,
                children: [
                  Text('Dark Mode'),
                  CustomToggleSwitch(
                    value: _darkModeEnabled,
                    onChanged: (value) {
                      setState(() => _darkModeEnabled = value);
                    },
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```
  </Tabs.Tab>
  <Tabs.Tab>

**Step 1:** Create the app structure

```dart
import 'package:flutter/material.dart';
import 'package:primitive_ui/primitive_ui.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Primitive UI Demo',
      home: SettingsPage(),
    );
  }
}
```

**Step 2:** Create a stateful widget for settings

```dart
class SettingsPage extends StatefulWidget {
  @override
  State<SettingsPage> createState() => _SettingsPageState();
}
```

**Step 3:** Add state variables

```dart
class _SettingsPageState extends State<SettingsPage> {
  bool _notificationsEnabled = true;
  bool _darkModeEnabled = false;

  // build method below...
}
```

**Step 4:** Build the UI using Primitive UI components

```dart
@override
Widget build(BuildContext context) {
  return Scaffold(
    appBar: AppBar(title: Text('Settings')),
    body: Padding(
      padding: EdgeInsets.all(16.0),
      child: VStack(
        spacing: 16.0,
        children: [
          _buildSettingItem('Notifications', _notificationsEnabled, (v) {
            setState(() => _notificationsEnabled = v);
          }),
          _buildSettingItem('Dark Mode', _darkModeEnabled, (v) {
            setState(() => _darkModeEnabled = v);
          }),
        ],
      ),
    ),
  );
}

Widget _buildSettingItem(
  String label,
  bool value,
  ValueChanged<bool> onChanged,
) {
  return CustomCard(
    elevation: 2.0,
    child: Row(
      mainAxisAlignment: MainAxisAlignment.spaceBetween,
      children: [
        Text(label),
        CustomToggleSwitch(
          value: value,
          onChanged: onChanged,
        ),
      ],
    ),
  );
}
```

  </Tabs.Tab>
</Tabs>

## Available Components

Primitive UI includes 4 core components:

### UI Components

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 my-6">
  <a href="/components/custom-card" className="block p-4 border rounded-lg hover:border-blue-500 transition-colors">
    <h3 className="text-lg font-semibold mb-2">CustomCard</h3>
    <span className="text-sm text-gray-600 dark:text-gray-400">
      Container with shadow, rounded corners, and padding
    </span>
  </a>
  <a href="/components/custom-toggle-switch" className="block p-4 border rounded-lg hover:border-blue-500 transition-colors">
    <h3 className="text-lg font-semibold mb-2">CustomToggleSwitch</h3>
    <span className="text-sm text-gray-600 dark:text-gray-400">
      Animated toggle switch with smooth transitions
    </span>
  </a>
  <a href="/components/custom-slider" className="block p-4 border rounded-lg hover:border-blue-500 transition-colors">
    <h3 className="text-lg font-semibold mb-2">CustomSlider</h3>
    <span className="text-sm text-gray-600 dark:text-gray-400">
      Slider for selecting a value from a range
    </span>
  </a>
  <a href="/components/custom-circular-progress" className="block p-4 border rounded-lg hover:border-blue-500 transition-colors">
    <h3 className="text-lg font-semibold mb-2">CustomCircularProgress</h3>
    <span className="text-sm text-gray-600 dark:text-gray-400">
      Indeterminate circular progress indicator
    </span>
  </a>
</div>

### Layout Components

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 my-6">
  <a href="/components/vstack" className="block p-4 border rounded-lg hover:border-blue-500 transition-colors">
    <h3 className="text-lg font-semibold mb-2">VStack</h3>
    <span className="text-sm text-gray-600 dark:text-gray-400">
      Vertical stack with spacing and alignment
    </span>
  </a>
  <a href="/components/hstack" className="block p-4 border rounded-lg hover:border-blue-500 transition-colors">
    <h3 className="text-lg font-semibold mb-2">HStack</h3>
    <span className="text-sm text-gray-600 dark:text-gray-400">
      Horizontal stack with spacing and alignment
    </span>
  </a>
  <a href="/components/zstack" className="block p-4 border rounded-lg hover:border-blue-500 transition-colors">
    <h3 className="text-lg font-semibold mb-2">ZStack</h3>
    <span className="text-sm text-gray-600 dark:text-gray-400">
      Layered stack for z-ordering widgets
    </span>
  </a>
</div>

## Quick Reference

### Common Patterns

**Creating a card:**
```dart
CustomCard(
  child: Text('Content'),
)
```

**Vertical layout:**
```dart
VStack(
  spacing: 16.0,
  children: [
    Text('First'),
    Text('Second'),
  ],
)
```

**Layering widgets:**
```dart
ZStack(
  alignment: Alignment.center,
  children: [
    Container(width: 200, height: 200, color: Colors.blue),
    Icon(Icons.star, size: 50),
  ],
)
```

**Toggle switch:**
```dart
CustomToggleSwitch(
  value: isEnabled,
  onChanged: (value) => setState(() => isEnabled = value),
)
```

## Next Steps

Now that you have Primitive UI installed, explore the documentation:

<div className="grid grid-cols-1 md:grid-cols-2 gap-6 my-8 not-prose">
  <a 
    href="/components/custom-card" 
    className="group relative flex flex-col gap-3 p-6 rounded-xl border-2 border-blue-200 dark:border-blue-700 bg-linear-to-br from-blue-50 to-blue-100/80 dark:from-blue-950/50 dark:to-blue-900/30 hover:border-blue-400 dark:hover:border-blue-500 hover:shadow-lg hover:shadow-blue-100 dark:hover:shadow-blue-900/20 transition-all duration-300 hover:-translate-y-1"
  >
    <div className="flex items-center gap-3">
      <span className="text-3xl" role="img" aria-label="Books">📚</span>
      <h3 className="text-xl font-bold text-blue-950 dark:text-blue-50 m-0">Component Documentation</h3>
    </div>
    <p className="text-sm text-blue-800 dark:text-blue-200 leading-relaxed m-0">
      Learn about each component's API and see examples
    </p>
  </a>

  <a 
    href="/examples/basic-usage" 
    className="group relative flex flex-col gap-3 p-6 rounded-xl border-2 border-amber-200 dark:border-amber-700 bg-linear-to-br from-amber-50 to-amber-100/80 dark:from-amber-950/50 dark:to-amber-900/30 hover:border-amber-400 dark:hover:border-amber-500 hover:shadow-lg hover:shadow-amber-100 dark:hover:shadow-amber-900/20 transition-all duration-300 hover:-translate-y-1"
  >
    <div className="flex items-center gap-3">
      <span className="text-3xl" role="img" aria-label="Lightbulb">💡</span>
      <h3 className="text-xl font-bold text-amber-950 dark:text-amber-50 m-0">Examples</h3>
    </div>
    <p className="text-sm text-amber-800 dark:text-amber-200 leading-relaxed m-0">
      Explore practical examples and common patterns
    </p>
  </a>

  <a 
    href="/architecture/primitives-explained" 
    className="group relative flex flex-col gap-3 p-6 rounded-xl border-2 border-purple-200 dark:border-purple-700 bg-linear-to-br from-purple-50 to-purple-100/80 dark:from-purple-950/50 dark:to-purple-900/30 hover:border-purple-400 dark:hover:border-purple-500 hover:shadow-lg hover:shadow-purple-100 dark:hover:shadow-purple-900/20 transition-all duration-300 hover:-translate-y-1"
  >
    <div className="flex items-center gap-3">
      <span className="text-3xl" role="img" aria-label="Building">🏗️</span>
      <h3 className="text-xl font-bold text-purple-950 dark:text-purple-50 m-0">Architecture</h3>
    </div>
    <p className="text-sm text-purple-800 dark:text-purple-200 leading-relaxed m-0">
      Understand how Primitive UI works under the hood
    </p>
  </a>

  <a 
    href="https://github.com/Berkay2002/TDDC73/tree/main/project/primitive_ui" 
    className="group relative flex flex-col gap-3 p-6 rounded-xl border-2 border-slate-200 dark:border-slate-700 bg-linear-to-br from-slate-50 to-slate-100/80 dark:from-slate-900/50 dark:to-slate-800/30 hover:border-slate-400 dark:hover:border-slate-500 hover:shadow-lg hover:shadow-slate-100 dark:hover:shadow-slate-900/20 transition-all duration-300 hover:-translate-y-1"
    target="_blank"
    rel="noopener noreferrer"
  >
    <div className="flex items-center gap-3">
      <span className="text-3xl" role="img" aria-label="Gear">⚙️</span>
      <h3 className="text-xl font-bold text-slate-950 dark:text-slate-50 m-0">Source Code</h3>
    </div>
    <p className="text-sm text-slate-800 dark:text-slate-200 leading-relaxed m-0">
      View the source code on GitHub
    </p>
  </a>
</div>

## Troubleshooting

### Package Not Found

If you see `Error: Cannot find package 'primitive_ui'`:

1. Verify the path in `pubspec.yaml` is correct
2. Run `flutter pub get`
3. Restart your IDE

### Import Errors

If you see "Undefined class" errors:

1. Ensure you've imported: `import 'package:primitive_ui/primitive_ui.dart';`
2. Run `flutter pub get`
3. Restart the Dart Analysis Server

### Shadow Not Rendering

If `CustomCard` shadows don't appear:

1. Add padding around the card to give space for the shadow
2. Ensure the card isn't clipped by its parent
3. Try increasing the `elevation` value

## Getting Help

Need assistance? Here are the best resources:

<div className="grid grid-cols-1 md:grid-cols-3 gap-4 my-6 not-prose">
  <a 
    href="https://github.com/Berkay2002/TDDC73/issues" 
    className="flex items-start gap-4 p-5 rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900/50 hover:border-blue-400 dark:hover:border-blue-600 hover:shadow-md transition-all duration-200"
    target="_blank"
    rel="noopener noreferrer"
  >
    <span className="text-2xl shrink-0" role="img" aria-label="Bug">🐛</span>
    <div>
      <h3 className="text-base font-semibold text-gray-900 dark:text-gray-100 mb-1 m-0">Report Issues</h3>
      <p className="text-sm text-gray-700 dark:text-gray-300 m-0">Found a bug? Let us know on GitHub</p>
    </div>
  </a>

  <a 
    href="https://github.com/Berkay2002/TDDC73/tree/main/project/primitive_ui/example" 
    className="flex items-start gap-4 p-5 rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900/50 hover:border-blue-400 dark:hover:border-blue-600 hover:shadow-md transition-all duration-200"
    target="_blank"
    rel="noopener noreferrer"
  >
    <span className="text-2xl shrink-0" role="img" aria-label="Code">💻</span>
    <div>
      <h3 className="text-base font-semibold text-gray-900 dark:text-gray-100 mb-1 m-0">View Examples</h3>
      <p className="text-sm text-gray-700 dark:text-gray-300 m-0">Check example code and usage</p>
    </div>
  </a>

  <a 
    href="https://github.com/Berkay2002/TDDC73/tree/main/project/primitive_ui/test" 
    className="flex items-start gap-4 p-5 rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900/50 hover:border-blue-400 dark:hover:border-blue-600 hover:shadow-md transition-all duration-200"
    target="_blank"
    rel="noopener noreferrer"
  >
    <span className="text-2xl shrink-0" role="img" aria-label="Test">🧪</span>
    <div>
      <h3 className="text-base font-semibold text-gray-900 dark:text-gray-100 mb-1 m-0">Review Tests</h3>
      <p className="text-sm text-gray-700 dark:text-gray-300 m-0">See test files for component usage</p>
    </div>
  </a>
</div>

---

**Ready to learn more?** Continue to the [Component Documentation](/components/custom-card) or explore the [Architecture](/architecture/primitives-explained) section.
